" Show Line Numbers"
set number

" Start Bar"
set laststatus=2

" Set compatibility to vim only "
set nocompatible

" Automatically wrap text that extends beyond the screen length"
set wrap

"Encoding "
set encoding=utf-8

